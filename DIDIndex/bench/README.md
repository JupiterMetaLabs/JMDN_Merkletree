# DID Index benchmarks

Documentation for the **TestFullReport** table, workload, and column meanings lives in the repository root **[README.md](../../README.md#did-index-benchmark)**.

Quick run:

```bash
go test -v -run TestFullReport ./DIDIndex/bench/
```
